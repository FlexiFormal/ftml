[tasks.test-no-features]
command = "cargo"
install_crate = false
args = ["+nightly", "test", "--no-default-features"]

[tasks.test-wasm]
command = "cargo"
install_crate = false
args = ["+nightly", "test", "--features=wasm"]

[tasks.test-reqwest]
command = "cargo"
install_crate = false
args = ["+nightly", "test", "--features=reqwest"]

[tasks.test-typescript]
command = "cargo"
install_crate = false
args = ["+nightly", "test", "--features=typescript"]

[tasks.test-server_fn]
command = "cargo"
install_crate = false
args = ["+nightly", "test", "--features=server_fn"]

[tasks.test-cached]
command = "cargo"
install_crate = false
args = ["+nightly", "test", "--features=cached"]

[tasks.test-deepsize]
command = "cargo"
install_crate = false
args = ["+nightly", "test", "--features=deepsize"]

[tasks.test-all-features]
command = "cargo"
install_crate = false
args = ["+nightly", "test", "--all-features"]

[tasks.all-tests]
dependencies = [
    "test-no-features",
    "test-wasm",
    "test-reqwest",
    "test-typescript",
    "test-server_fn",
    "test-cached",
    "test-all-features",
]

[tasks.doc]
command = "cargo"
install_crate = false
args = ["+nightly", "doc", "--no-deps", "--all-features"]
